import { NgModule } from '@angular/core';
import { TestingAsynchronousCodeComponent } from './testingAsynchronousCode.component';

@NgModule({
    declarations: [
        TestingAsynchronousCodeComponent,
    ],
    imports: [
    ],
    exports: [
        TestingAsynchronousCodeComponent,
    ],
})
export class TestingAsynchronousCodeModule { }
